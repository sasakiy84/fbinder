# 基本方針

このファイルは、コーディングエージェントがこのリポジトリで作業するときの恒常的な作業規約である。

優先順位は以下の通り。

1. ユーザーからの明示的な指示
2. 作業対象に最も近い `AGENTS.md`
3. リポジトリルートの `AGENTS.md`
4. 既存コードの設計・命名・テスト方針

指示が衝突する場合は、勝手に解釈して進めず、衝突内容と選択肢を示してユーザーに確認する。


# 作業開始前の確認

実装前に以下を確認する。

- 変更対象ファイルの周辺コード
- 既存の類似実装
- 関連するテスト
- README / CONTRIBUTING / docs など、該当機能に関係する文書
- package manager、test runner、formatter、linter の既存設定

# 変更範囲

- 依頼された目的に直接関係する変更だけを行う。
- ついでのリファクタリング、フォーマット変更、命名変更、ファイル移動は行わない。
- 必要な改善を見つけた場合は、今回の変更に混ぜず、別提案として報告する。そのとき、後から参照できるように、提案には記号で参照できるIDをつける。
- 既存の public API、設定ファイル形式、データ構造、DB スキーマを変更する場合は、事前にユーザーへ確認する。

# コーディング
- 既存コードの命名、責務分割、エラー処理、テストの書き方を優先する。
- 新しい抽象化は、同じパターンが複数箇所で必要になってから導入する。
- 過度に複雑なコードは避ける。たとえば、Python を使っているときに二重のリスト内包表記を使ってはいけない
- 将来の拡張を見越した過剰な一般化は避ける。
- 一時的な回避策には、理由・影響範囲・解消条件を書く。
- 定数や型エイリアスは、ファイル冒頭に置く必然性がない場合、利用箇所の近くに閉じ込める
- 必ず型を明示する。関数の引数や返り値にも型をつける。このとき、Any などの曖昧な型は避け、できるだけ具体的な型をつける
- 関数は細かく切りすぎない。ユーザーレビューのときに、関数が分割されすぎていると、コードジャンプが多くて読みづらい
- 意味的に１つの処理としてまとまっているならば、関数が長くなってしまっても構わない
- コメントは「何をしているか」ではなく「なぜそうしているか」を説明する
- コードを読めば分かる処理説明コメントは避ける。
- 仕様上の制約、外部 API の癖、互換性維持、過去の不具合回避などはコメントで明示する。

# 開発フロー
- ユーザーに意見を求めるときは、記号で参照できる選択肢を提示する。そのとき、前の選択肢を参照できるように、最初の質問は A1, A2、次の質問は B1, B2 などとする
- それぞれの選択肢には内容とともに、メリットとデメリットを簡潔に書く。特に推薦するものがあるときは、記号のあとに(Recommended) と書く
- 大規模な変更は、`plans/` 以下に `p0000_TITLE.md` のようなファイルを作成して、変更の目的、内容、影響範囲などを説明する。変更の内容が複雑な場合は、複数のファイルに分けて説明する
- 複数のファイルをまたぐような機能は、`docs/` 以下に `d0000_TITLE.md` のようなファイルを作成して、機能の目的、利用方法、設計方針などを説明する。機能を変更するときは、関連するドキュメントも更新する
- ファイルの冒頭には、ファイルの目的、役割、利用方法などを簡潔に書いておく。特に、ユーザーが直接触れる可能性のあるファイルは、利用方法を明示する。機能を変更するときは、関連するファイルの冒頭も更新する
- 起動方法、フォルダごとの役割などは `README.md` に簡潔に書いておく

# テスト
- テストはドキュメントとしてのテストを心がける。どのような入力に対してどのような出力が期待されるのかを明確にする。テストコードは、テストケースごとに分けて書く
- テスト名は、仕様として読める名前にする。
- 1つのテストケースでは、1つの振る舞いだけを検証する。
- 正常系だけでなく、境界値、空入力、異常系、既存バグの再発防止を必要に応じて追加する。
- mock は最小限にし、重要な変換・分岐・副作用は実際の振る舞いに近い形で検証する。
- テストのためだけに本番コードを不自然に歪めない。
- バグをドキュメント化できることはテストの意義の1つである

# Git / Commit ルール

- 1 commit には 1つの意味的な変更だけを含める。
- 例: スキーマ変更、警告対応、リファクタリング、テスト追加は必要に応じて別 commit に分ける。
- コミット前に必ず差分を確認し、今回の変更に関係ないファイルを stage しない。
- 作業前から存在する未コミット変更は、明示的に依頼されない限り触らない・戻さない・コミットに混ぜない。
- コミットメッセージは日本語で書く。
- コミットメッセージは必ず複数行にし、1行目に要約、本文に変更の意図を書く。
- コミット前には、変更内容に応じた確認コマンドを自分で実行し、通ったことを確認してから報告する。
- １コミットの意味的なまとまりを保つために、既存の順番、関数分割、ファイル構成などを、主目的と関係なく変更しない。
- 付随する改善を見つけても、同じ commit に混ぜず、必要なら別 commit に分ける。
- コミットメッセージの形式は以下を基本とする。

```text
変更内容の短い要約

なぜこの変更が必要かを書く。

実装上の意図や、避けたい問題があれば書く。
```

# 検証

変更内容に応じて、最小限かつ十分な確認を行う。

- ロジック変更: 関連する unit test を実行する。
- 型変更: typecheck を実行する。
- UI 変更: 該当画面の表示確認、または snapshot / component test を実行する。
- API 変更: request / response の例、互換性、エラーケースを確認する。
- ドキュメント変更のみ: リンク、コマンド、コード例が現状と矛盾しないか確認する。
- 確認できなかった場合は、実行したコマンド、失敗理由、未確認のリスクを報告する。

# Do Not

- 明示的に依頼されていない大規模リファクタリングをしない。
- テストを通すために仕様を弱めない。
- 型エラーや lint エラーを `ignore`、`Any`、型アサーションで安易に回避しない。
- エラーを握りつぶさない。
- secret、token、credential、個人情報をログやテストデータに含めない。
- 失敗したコマンドを、原因分析なしに何度も繰り返さない。
